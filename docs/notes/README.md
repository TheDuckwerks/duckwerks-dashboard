# notes/

Ephemera: one-off research, consumed working notes, and loose data blobs that aren't referenced by code. Not maintained, not load-bearing, not linked from the first-class docs. Safe to prune. If something here turns out to be load-bearing reference, promote it out to a proper home (`docs/`, a data dir, or wherever its consumer lives).
